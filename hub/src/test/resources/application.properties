# 测试专用覆盖：公共 Proxy 源的上游指向不可达本地地址，使默认测试上下文启动时快失败源降级
# （ADR-0005 / #35 源降级），避免每次测试启动都尝试连接生产 MCP Server。
# 专项测试 McpProxySingleEndpointTest 经 DynamicPropertySource 覆盖为内嵌上游。
# 注意：每新增一个免认证 public proxy provider，必须在此登记其 upstream-url，
# 否则所有其它 @SpringBootTest 上下文会尝试直连生产 MCP Server。
proxy.context7.upstream-url=http://localhost:1/mcp
proxy.grep-app.upstream-url=http://localhost:1/mcp
proxy.wikidata.upstream-url=http://localhost:1/mcp
# markitdown 现为 ContainerMcp 容器源（#37，#38 起 content 引擎已退役）：docker 运行时/清单缺失时
# 该源自动降级禁用，无需在此额外配置（专项测试经 docker.enabled=false + fake ContainerManager 隔离）。
